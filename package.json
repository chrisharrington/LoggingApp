{
  "name": "loggingapp",
  "subdomain": "loggingapp",
  "scripts": {
    "test": "mocha --recursive",
    "coverage": "istanbul cover _mocha -- --recursive && open coverage/lcov-report/index.html",
    "encrypt": "encrypt secureConfig.json secureConfig.json.cast5",
    "decrypt": "decrypt secureConfig.json.cast5 secureConfig.json"
  },
  "version": "0.0.1",
  "engines": {
    "node": "0.10.x"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/chrisharrington/Logger.git"
  },
  "dependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-concat": "^0.5.0",
    "grunt-contrib-uglify": "^0.5.1",
    "grunt-css": "^0.5.4",
    "grunt-contrib-less": "^0.11.3",
    "grunt-contrib-watch": "^0.6.1"
  }
}
